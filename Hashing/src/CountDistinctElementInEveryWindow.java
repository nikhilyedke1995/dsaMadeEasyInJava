public class CountDistinctElementInEveryWindow {
    public static void main(String[] args) {
        /**
         * TODO
         * Create a frequency map of first K items
         * Print size of map
         * then run loop for(int i=k; k<nl i++)
         * 1. Decrease the frequency of arr[i-k]
         * 2. if the frequency of arr[i-k] becomes 0, remove it from the map
         * 3. if arr[i] does not exist in the map, insert it, else increase its frequency in the map
         * 4. print the size of map
         * Time complexity O(n)
         */
    }
}
